syntax = "proto3";

package orznewbie.test;

option go_package = "github.com/orznewbie/test;test";

message Request {
  string input = 1;
}

message Response {
  string content_type = 1;

  // The HTTP request/response body as raw binary.
  bytes data = 2;
}